GitHub Pages video-cache fix package

This package renames all five web-compatible MP4 files to *-v2.mp4 and updates index.html accordingly.
Use this when the old MP4 URLs are cached or GitHub Pages is serving a stale asset.

Copy the contents of this package to the branch that GitHub Pages is serving (likely gh-pages), commit, and push.
